import 'package:flutter/cupertino.dart';

class ScreenAnalysis extends StatelessWidget {
  const ScreenAnalysis({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Analysis'));
  }
}
